<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>台股情報站 | THINK BIG Lab</title>
<meta name="robots" content="noindex, nofollow">
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{background:#0a0f1e;color:#e2e8f0;font-family:-apple-system,sans-serif;min-height:100vh}
.lock-screen{display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:100vh;padding:40px}
.logo{font-size:56px;margin-bottom:16px}
h1{font-size:26px;color:#60a5fa;margin-bottom:6px;font-weight:700}
.subtitle{color:#94a3b8;margin-bottom:36px;font-size:14px}
input[type=password]{background:#1e293b;border:1px solid #334155;color:#e2e8f0;padding:14px 20px;border-radius:10px;font-size:16px;width:300px;display:block;margin:0 auto 12px;text-align:center;letter-spacing:6px}
input[type=password]:focus{outline:none;border-color:#60a5fa}
input[type=text],textarea{background:#1e293b;border:1px solid #334155;color:#e2e8f0;padding:10px 14px;border-radius:8px;font-size:14px;width:100%;margin-top:8px}
textarea{height:80px;resize:vertical}
.btn{background:#2563eb;color:#fff;border:none;padding:14px 40px;border-radius:10px;font-size:16px;cursor:pointer;width:300px;font-weight:600}
.btn:hover{background:#1d4ed8}
.btn-sm{background:#1e40af;color:#fff;border:none;padding:8px 20px;border-radius:8px;font-size:13px;cursor:pointer;margin-top:8px;width:100%}
.btn-sm:hover{background:#1e3a8a}
.error{color:#f87171;font-size:13px;margin-top:10px}
.success{color:#34d399;font-size:13px;margin-top:6px}
.dashboard{display:none;padding:24px;max-width:1100px;margin:0 auto}
.header{display:flex;justify-content:space-between;align-items:center;margin-bottom:28px;padding-bottom:16px;border-bottom:1px solid #1e293b}
.header h1{font-size:20px;color:#60a5fa}
.badge{background:#1e293b;padding:4px 14px;border-radius:20px;font-size:12px;color:#94a3b8}
.section{margin-bottom:32px}
.section-title{font-size:16px;font-weight:700;color:#93c5fd;margin-bottom:14px;padding-left:10px;border-left:3px solid #2563eb}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:14px;margin-bottom:14px}
.card{background:#1e293b;border-radius:12px;padding:20px;border:1px solid #334155}
.card h3{font-size:12px;color:#94a3b8;margin-bottom:8px;text-transform:uppercase;letter-spacing:1px}
.card .value{font-size:26px;font-weight:700;color:#e2e8f0}
.card .sub{font-size:12px;color:#64748b;margin-top:4px}
.up{color:#34d399}.down{color:#f87171}
.stock-search{display:flex;gap:10px;margin-bottom:14px}
.stock-search input{flex:1;background:#1e293b;border:1px solid #334155;color:#e2e8f0;padding:12px 16px;border-radius:10px;font-size:15px}
.stock-search input:focus{outline:none;border-color:#60a5fa}
.stock-search button{background:#2563eb;color:#fff;border:none;padding:12px 24px;border-radius:10px;font-size:15px;cursor:pointer;white-space:nowrap}
.stock-result{background:#1e293b;border-radius:12px;padding:20px;border:1px solid #334155;margin-bottom:14px;display:none}
.stock-result h2{font-size:20px;font-weight:700;margin-bottom:4px}
.stock-meta{font-size:13px;color:#94a3b8;margin-bottom:16px}
.stock-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
.stock-item{background:#0f172a;border-radius:8px;padding:12px}
.stock-item .label{font-size:11px;color:#64748b;margin-bottom:4px}
.stock-item .val{font-size:16px;font-weight:600}
.rank-list{background:#1e293b;border-radius:12px;border:1px solid #334155;overflow:hidden;margin-bottom:14px}
.rank-item{display:flex;align-items:center;padding:12px 16px;border-bottom:1px solid #0f172a;gap:12px}
.rank-item:last-child{border-bottom:none}
.rank-num{font-size:13px;color:#64748b;width:24px}
.rank-name{flex:1;font-size:14px;font-weight:600}
.rank-sub{font-size:12px;color:#94a3b8}
.rank-val{font-size:14px;font-weight:700}
.ai-card{background:#1e293b;border-radius:12px;padding:20px;border:1px solid #334155;margin-bottom:14px}
.ai-card .ai-tag{font-size:11px;background:#1e40af;color:#93c5fd;padding:2px 8px;border-radius:20px;display:inline-block;margin-bottom:10px}
.ai-card .ai-text{font-size:14px;line-height:1.8;color:#cbd5e1}
.ai-card .ai-stock{font-weight:700;color:#60a5fa}
.feedback-box{background:#0f172a;border-radius:10px;padding:14px;margin-top:12px;border:1px dashed #334155}
.feedback-box p{font-size:12px;color:#64748b;margin-bottom:6px}
.coming{background:#1e293b;border-radius:12px;padding:50px 20px;text-align:center;border:1px dashed #334155;color:#475569;margin-bottom:14px}
.coming span{font-size:40px;display:block;margin-bottom:10px}
.tag{display:inline-block;padding:2px 8px;border-radius:20px;font-size:11px;margin-right:4px}
.tag-up{background:#064e3b;color:#34d399}
.tag-down{background:#450a0a;color:#f87171}
.tag-neutral{background:#1e293b;color:#94a3b8}
</style>
</head>
<body>

<div class="lock-screen" id="lockScreen">
  <div class="logo">🦞</div>
  <h1>台股情報站</h1>
  <p class="subtitle">THINK BIG Lab · 內部測試版</p>
  <input type="password" id="pwInput" placeholder="輸入密碼" onkeydown="if(event.key==='Enter')checkPw()">
  <button class="btn" onclick="checkPw()">進入</button>
  <div class="error" id="errMsg"></div>
</div>

<div class="dashboard" id="dashboard">
  <div class="header">
    <h1>🦞 台股情報站</h1>
    <span class="badge">內部測試版 · THINK BIG Lab</span>
  </div>

  <!-- 大盤總覽 -->
  <div class="section">
    <div class="section-title">📊 大盤總覽</div>
    <div class="grid">
      <div class="card">
        <h3>加權指數</h3>
        <div class="value" id="taiex">載入中...</div>
        <div class="sub" id="taiexChange">—</div>
      </div>
      <div class="card">
        <h3>今日成交量</h3>
        <div class="value" id="volume">載入中...</div>
        <div class="sub">億元</div>
      </div>
      <div class="card">
        <h3>外資今日</h3>
        <div class="value" id="foreign">載入中...</div>
        <div class="sub">億元買賣超</div>
      </div>
    </div>
    <div class="feedback-box">
      <p>💬 對「大盤總覽」的建議：</p>
      <textarea id="fb1" placeholder="例如：希望加入台指期、美股連動指標..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('大盤總覽','fb1')">送出建議</button>
      <div class="success" id="fb1ok"></div>
    </div>
  </div>

  <!-- 個股搜尋 -->
  <div class="section">
    <div class="section-title">🔍 個股搜尋</div>
    <div class="stock-search">
      <input type="text" id="stockInput" placeholder="輸入股票代號，例如：2330" onkeydown="if(event.key==='Enter')searchStock()">
      <button onclick="searchStock()">查詢</button>
    </div>
    <div class="stock-result" id="stockResult">
      <h2 id="stockName">—</h2>
      <div class="stock-meta" id="stockMeta">—</div>
      <div class="stock-grid">
        <div class="stock-item"><div class="label">收盤價</div><div class="val" id="sClose">—</div></div>
        <div class="stock-item"><div class="label">漲跌幅</div><div class="val" id="sChange">—</div></div>
        <div class="stock-item"><div class="label">成交量</div><div class="val" id="sVol">—</div></div>
        <div class="stock-item"><div class="label">開盤</div><div class="val" id="sOpen">—</div></div>
        <div class="stock-item"><div class="label">最高</div><div class="val" id="sHigh">—</div></div>
        <div class="stock-item"><div class="label">最低</div><div class="val" id="sLow">—</div></div>
      </div>
    </div>
    <div class="feedback-box">
      <p>💬 對「個股搜尋」的建議：</p>
      <textarea id="fb2" placeholder="例如：希望加入K線圖、法人籌碼、AI解讀..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('個股搜尋','fb2')">送出建議</button>
      <div class="success" id="fb2ok"></div>
    </div>
  </div>

  <!-- AI每日解讀 -->
  <div class="section">
    <div class="section-title">🤖 AI每日解讀</div>
    <div class="ai-card">
      <span class="ai-tag">AI 生成 · 僅供參考</span>
      <div class="ai-text">
        <span class="ai-stock">台積電 (2330)</span> 今日外資買超逾5,000張，站上月線，技術面轉強。近期AI伺服器需求持續升溫，法人樂觀看待下季營收。<br><br>
        <span class="ai-stock">鴻海 (2317)</span> 籌碼面出現大戶承接訊號，PTT討論熱度為近30日最高，短線有表態機會。<br><br>
        <span class="ai-stock">聯發科 (2454)</span> 本週投信連續買超，基本面穩健，適合中長線關注。
      </div>
      <div style="margin-top:12px;font-size:11px;color:#475569">⚠️ 以上內容為AI自動生成，不構成投資建議，投資有風險請自行判斷</div>
    </div>
    <div class="feedback-box">
      <p>💬 對「AI每日解讀」的建議：</p>
      <textarea id="fb3" placeholder="例如：希望能自訂追蹤的股票、解讀內容太短/太長..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('AI每日解讀','fb3')">送出建議</button>
      <div class="success" id="fb3ok"></div>
    </div>
  </div>

  <!-- 輿情雷達 -->
  <div class="section">
    <div class="section-title">📡 輿情雷達（PTT討論熱度）</div>
    <div class="rank-list" id="sentimentList">
      <div class="rank-item"><div class="rank-num">1</div><div><div class="rank-name">台積電 2330</div><div class="rank-sub">今日討論 142 則</div></div><span class="tag tag-up">正面</span></div>
      <div class="rank-item"><div class="rank-num">2</div><div><div class="rank-name">鴻海 2317</div><div class="rank-sub">今日討論 89 則</div></div><span class="tag tag-up">正面</span></div>
      <div class="rank-item"><div class="rank-num">3</div><div><div class="rank-name">聯發科 2454</div><div class="rank-sub">今日討論 67 則</div></div><span class="tag tag-neutral">中性</span></div>
      <div class="rank-item"><div class="rank-num">4</div><div><div class="rank-name">廣達 2382</div><div class="rank-sub">今日討論 54 則</div></div><span class="tag tag-up">正面</span></div>
      <div class="rank-item"><div class="rank-num">5</div><div><div class="rank-name">緯創 3231</div><div class="rank-sub">今日討論 41 則</div></div><span class="tag tag-down">負面</span></div>
    </div>
    <div class="feedback-box">
      <p>💬 對「輿情雷達」的建議：</p>
      <textarea id="fb4" placeholder="例如：希望加入爆料同學會、Threads、YouTube留言..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('輿情雷達','fb4')">送出建議</button>
      <div class="success" id="fb4ok"></div>
    </div>
  </div>

  <!-- 三大法人 -->
  <div class="section">
    <div class="section-title">🏦 三大法人今日買超排行</div>
    <div class="rank-list" id="institutionalList">
      <div class="rank-item"><div class="rank-num">1</div><div><div class="rank-name">台積電 2330</div><div class="rank-sub">外資</div></div><div class="rank-val up">+8,420張</div></div>
      <div class="rank-item"><div class="rank-num">2</div><div><div class="rank-name">鴻海 2317</div><div class="rank-sub">投信</div></div><div class="rank-val up">+3,210張</div></div>
      <div class="rank-item"><div class="rank-num">3</div><div><div class="rank-name">廣達 2382</div><div class="rank-sub">外資</div></div><div class="rank-val up">+2,890張</div></div>
      <div class="rank-item"><div class="rank-num">4</div><div><div class="rank-name">聯發科 2454</div><div class="rank-sub">外資+投信</div></div><div class="rank-val up">+1,560張</div></div>
      <div class="rank-item"><div class="rank-num">5</div><div><div class="rank-name">台達電 2308</div><div class="rank-sub">投信</div></div><div class="rank-val up">+980張</div></div>
    </div>
    <div class="feedback-box">
      <p>💬 對「三大法人」的建議：</p>
      <textarea id="fb5" placeholder="例如：希望看到賣超排行、分開顯示外資/投信/自營..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('三大法人','fb5')">送出建議</button>
      <div class="success" id="fb5ok"></div>
    </div>
  </div>

  <!-- 技術指標提醒 -->
  <div class="section">
    <div class="section-title">🔔 技術指標提醒（建置中）</div>
    <div class="coming">
      <span>🔨</span>
      設定條件（RSI低於20、KD黃金交叉等）→ 自動發Email通知<br>
      <small style="font-size:12px;margin-top:8px;display:block">預計下一版本上線</small>
    </div>
    <div class="feedback-box">
      <p>💬 你最希望收到哪種提醒？</p>
      <textarea id="fb6" placeholder="例如：RSI低於30時通知我、外資連買3天通知我..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('技術指標提醒','fb6')">送出建議</button>
      <div class="success" id="fb6ok"></div>
    </div>
  </div>

  <!-- 選股條件 -->
  <div class="section">
    <div class="section-title">🎯 智慧選股（建置中）</div>
    <div class="coming">
      <span>🔨</span>
      自訂條件篩選：外資連買N天 + 站上季線 + 輿情正面<br>
      <small style="font-size:12px;margin-top:8px;display:block">預計下一版本上線</small>
    </div>
    <div class="feedback-box">
      <p>💬 你最常用什麼條件選股？</p>
      <textarea id="fb7" placeholder="例如：殖利率超過5%、本益比低於15、外資連買..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('智慧選股','fb7')">送出建議</button>
      <div class="success" id="fb7ok"></div>
    </div>
  </div>

  <!-- 整體建議 -->
  <div class="section">
    <div class="section-title">💡 整體建議</div>
    <div class="feedback-box" style="background:#1e293b;border:1px solid #334155">
      <p>還有什麼功能是你最想看到的？</p>
      <textarea id="fb8" placeholder="任何想法都歡迎，例如：希望有App版、希望能追蹤美股、希望有社群討論區..."></textarea>
      <button class="btn-sm" onclick="sendFeedback('整體建議','fb8')">送出建議</button>
      <div class="success" id="fb8ok"></div>
    </div>
  </div>

  <div style="text-align:center;padding:40px 0;color:#334155;font-size:12px">
    台股情報站 · THINK BIG Lab · 內部測試版<br>
    所有內容僅供參考，不構成投資建議
  </div>
</div>

<script>
const PW = 'thinkbig2025';
const SUPABASE_URL = 'https://sirhskxufayklqrlxeep.supabase.co';
const SUPABASE_KEY = 'sb_publishable_KEnwL0LHgUp7puGK1KvLCg_XMWCC14m';

function checkPw() {
  const input = document.getElementById('pwInput').value;
  if (input === PW) {
    localStorage.setItem('lab_auth', 'true');
    showDashboard();
  } else {
    document.getElementById('errMsg').textContent = '密碼錯誤，請再試一次';
  }
}

function showDashboard() {
  document.getElementById('lockScreen').style.display = 'none';
  document.getElementById('dashboard').style.display = 'block';
  loadMarketData();
}

if (localStorage.getItem('lab_auth') === 'true') showDashboard();

async function loadMarketData() {
  try {
    const r = await fetch('https://openapi.twse.com.tw/v1/exchangeReport/FMTQIK');
    const data = await r.json();
    if (data && data[0]) {
      const d = data[0];
      document.getElementById('taiex').textContent = parseFloat(d.TSE指數).toLocaleString();
      const change = parseFloat(d.TSE漲跌點數);
      const el = document.getElementById('taiexChange');
      el.textContent = (change >= 0 ? '▲ +' : '▼ ') + change.toFixed(2) + ' 點';
      el.className = 'sub ' + (change >= 0 ? 'up' : 'down');
      const vol = (parseFloat(d.成交金額) / 100000000).toFixed(0);
      document.getElementById('volume').textContent = Number(vol).toLocaleString();
    }
  } catch(e) {
    document.getElementById('taiex').textContent = '盤後更新';
  }
  try {
    const r2 = await fetch('https://openapi.twse.com.tw/v1/fund/TWT38U');
    const data2 = await r2.json();
    if (data2 && data2[0]) {
      const val = parseFloat(data2[0].外陸資買賣超股數);
      const billions = (val / 100000000).toFixed(1);
      const el2 = document.getElementById('foreign');
      el2.textContent = (val >= 0 ? '+' : '') + billions;
      el2.className = 'value ' + (val >= 0 ? 'up' : 'down');
    }
  } catch(e) {
    document.getElementById('foreign').textContent = '盤後更新';
  }
}

async function searchStock() {
  const code = document.getElementById('stockInput').value.trim();
  if (!code) return;
  try {
    const r = await fetch(`https://openapi.twse.com.tw/v1/exchangeReport/STOCK_DAY?stockNo=${code}`);
    const data = await r.json();
    if (data && data.length > 0) {
      const d = data[data.length - 1];
      document.getElementById('stockResult').style.display = 'block';
      document.getElementById('stockName').textContent = code;
      document.getElementById('stockMeta').textContent = `最新交易日：${d.Date}`;
      document.getElementById('sClose').textContent = d.ClosingPrice;
      const change = parseFloat(d.Change);
      const changeEl = document.getElementById('sChange');
      changeEl.textContent = (change >= 0 ? '+' : '') + d.Change;
      changeEl.className = 'val ' + (change >= 0 ? 'up' : 'down');
      document.getElementById('sVol').textContent = parseInt(d.TradeVolume).toLocaleString();
      document.getElementById('sOpen').textContent = d.OpeningPrice;
      document.getElementById('sHigh').textContent = d.HighestPrice;
      document.getElementById('sLow').textContent = d.LowestPrice;
    } else {
      alert('找不到此股票，請確認代號是否正確');
    }
  } catch(e) {
    alert('查詢失敗，請稍後再試');
  }
}

async function sendFeedback(section, inputId) {
  const message = document.getElementById(inputId).value.trim();
  if (!message) { alert('請先填寫建議內容'); return; }
  try {
    const r = await fetch(`${SUPABASE_URL}/rest/v1/feedback`, {
      method: 'POST',
      headers: {
        'apikey': SUPABASE_KEY,
        'Authorization': `Bearer ${SUPABASE_KEY}`,
        'Content-Type': 'application/json',
        'Prefer': 'return=minimal'
      },
      body: JSON.stringify({ section, message })
    });
    if (r.ok) {
      document.getElementById(inputId + 'ok').textContent = '✅ 已收到，謝謝你的建議！';
      document.getElementById(inputId).value = '';
    } else {
      alert('送出失敗，請稍後再試');
    }
  } catch(e) {
    alert('送出失敗，請稍後再試');
  }
}
</script>
</body>
</html>
